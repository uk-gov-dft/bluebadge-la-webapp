blue-badge:
  auth-server:
    url: http://localhost:8380/test-auth
  usermanagementservice:
    servicehost:
      host: localhost
      port: 8180
      contextpath: /test-usermanagementservice
spring:
  session:
    store-type: none
management:
  endpoint:
    health:
      show-details: always

