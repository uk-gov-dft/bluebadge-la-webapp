#!/usr/bin/env bash

# Override the version with the branch name
export LA_VERSION=$(cat VERSION-computed)
export AZ_VERSION=v0.20.0-feature_BBB-1384-la-admin-able-to-update-gov-uk-service-credentials
